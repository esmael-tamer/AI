You are a senior full-stack engineer building a production-ready MVP on Replit.

PROJECT GOAL
Build a complete platform for "Media Trend" that ships as:
1) Web App (SEO-friendly website + customer portal + admin portal) using Next.js
2) Mobile App (Expo React Native) connected to the same backend APIs

CONTACT INFO (MUST USE)
- Company: Media Trend
- Official WhatsApp/Phone: +9656566179840
- WhatsApp link base: https://wa.me/9656566179840
- Domain: {DOMAIN}   (use existing domain)
- Email: {EMAIL_OPTIONAL} (if unknown, set placeholder but keep optional)

PRIMARY MARKETS
Kuwait (KW) + Saudi (SA), but global-ready.

LANGUAGES
Arabic (RTL) + English (LTR), full i18n across web + mobile.

STYLE (OPTIONAL BUT IF PROVIDED MUST MATCH)
If these references are provided, extract tokens and match the look & feel:
- Reference #1: {REFERENCE_URL_1}
- Reference #2: {REFERENCE_URL_2}
Implement design tokens (CSS vars) and apply consistently. No copying assets/text 1:1.

========================================================
STACK / ARCHITECTURE (MANDATORY)
========================================================
Monorepo using pnpm workspaces (or npm workspaces if pnpm is hard):
- apps/web: Next.js 14+ App Router + TypeScript + Tailwind + shadcn/ui
- apps/mobile: Expo (React Native) + Expo Router + TypeScript
- packages/shared: shared types, validation schemas, utils

Backend:
- Use apps/web as the backend via Next.js Route Handlers under /api/*
- Use Prisma ORM + Replit SQL DATABASE_URL
- All secrets via Replit Secrets (env vars)

Auth:
- Implement email magic link OR OTP (choose simplest on Replit).
- Must support role: USER and ADMIN.
- Separate guards for /portal/* vs /admin/*.

AI:
- Use OpenAI API via server routes:
  - store builder chat
  - optional STT for mobile voice input
  - ads campaign brief generator
  - account management brief generator
Env:
- OPENAI_API_KEY

========================================================
BUSINESS MODEL (NON-NEGOTIABLE)
========================================================
- Unlimited products (no limits).
- No monthly/yearly subscriptions.
- Commission on sales ONLY.
- Integrations: payment gateway + shipping company + warehousing/fulfillment.
- Activation is workflow-based: user submits request -> admin approves -> setup -> live.

========================================================
CORE USER EXPERIENCE (DEMO -> LOGIN -> SUBMIT REQUEST -> TEAM CONTACT)
========================================================
A) DEMO FIRST (NO SIGNUP UPFRONT)
1) User enters Home and types OR speaks their store idea.
2) AI asks minimal missing questions (max 6–8 short questions).
3) Create demo store immediately as guest:
   - store.ownerId = null
   - store.sessionId = guestSessionId (cookie/localStorage)
   - store.status = "DEMO"
4) Redirect to demo storefront:
   /s/[storeSlug]?demo=1
5) Show the FULL storefront UI.

B) LOGIN ONLY AFTER DEMO IS VISIBLE
On demo store page show a sticky CTA:
- AR: "كمّل طلبك"
- EN: "Continue your request"
When clicked:
- Redirect to CUSTOMER portal auth:
  /portal/login or /portal/signup (not generic /login)

C) AFTER LOGIN: COMPLETE REQUEST (MANDATORY)
After auth success:
- Attach demo store to user:
  store.ownerId = user.id
  store.sessionId = null
- Redirect to:
  /portal/stores/[storeId]
Show a stepper "Complete Request":
Step A: Confirm store basics (store name, country, WhatsApp)
Step B: Choose activations needed:
  - payments_activation
  - shipping_activation
  - warehousing_activation (optional)
Step C: Collect required info:
  - Contact name
  - WhatsApp/phone
  - Email (optional)
  - Business details (short)
  - Pickup address (for shipping)
  - Notes
Step D: Submit

D) ON SUBMIT: SEND REQUEST TO MEDIA TREND + SAVE IN ADMIN
- Create Lead record + Tickets based on activation choices:
  tickets.status = "Pending"
- Update store.status = "PENDING_ACTIVATION"
- Show confirmation page with:
  - “Request received, our team will contact you”
  - WhatsApp click-to-chat button to Media Trend with a prefilled message (see templates below)
- Ensure the request appears immediately in Admin “New Requests”.

E) CHECKOUT RULE
Checkout remains DEMO-only until:
- paymentsStatus = "live" AND shippingStatus = "live"
Warehousing optional.

========================================================
ADDITIONAL SERVICES (NEW) — SEPARATE SERVICES
========================================================
Add two services as independent service offerings (NOT auto-build like store):
1) Launch Advertising Campaigns on All Platforms
2) Social Media Account Management

Behavior:
- Each service has its own page with an “AI Planner” widget:
  - User provides goal, budget range, platforms (Meta/Google/TikTok/Snapchat/X/LinkedIn), country, industry, timeframe.
  - AI outputs a structured Brief + estimated KPI ranges labeled as estimates.
- Submit requires login (customer portal):
  /portal/login
- On submit:
  Create Lead + Ticket:
   - type = ads_launch_request OR account_management_request
   - status = Pending
- Admin follows up.

========================================================
WEB ROUTES (MANDATORY)
========================================================
PUBLIC SITE
- /                 (Home + AI assistant hero)
- /services          (services list)
- /services/store-builder
- /services/ads-launch           (AI Planner + submit request)
- /services/account-management   (AI Planner + submit request)
- /work              (case studies)
- /partners
- /team
- /insights           (blog list)
- /insights/[slug]    (blog post)
- /contact
- /privacy /terms
- /p/[slug]           (published pages from Content Studio)

STOREFRONT
- /s/[storeSlug]      (demo/live storefront)

CUSTOMER PORTAL (SEPARATE ENTRY)
- /portal                 (Customer Portal landing)
- /portal/login
- /portal/signup
- /portal/dashboard
- /portal/stores
- /portal/stores/[storeId]
- /portal/requests
- /portal/support

ADMIN PORTAL (SEPARATE ENTRY)
- /admin/login
- /admin
- /admin/requests
- /admin/stores
- /admin/users
- /admin/integrations
- /admin/commission
- /admin/content/pages
- /admin/content/partners
- /admin/content/team
- /admin/content/work
- /admin/blog
- /admin/support
- /admin/settings
- /admin/audit

Hard rules:
- Non-admin can never access /admin routes.
- Admin cannot authenticate through /portal routes.

========================================================
HOME PAGE UI REQUIREMENTS
========================================================
Home must feel premium and organized.
Below the AI hero, add 4 stacked, well-designed carousels/sliders (one under another):
1) Our Departments (or Team/Departments)
2) Our Work / Case Studies
3) Our Services
4) Partners (logos)
Each has “View all” linking to /team, /work, /services, /partners.

========================================================
MOBILE APP (EXPO) — REQUIRED
========================================================
Mobile app is CUSTOMER ONLY in MVP (Admin web-only).
Must include:
- Auth screens:
  /login, /signup
- Dashboard:
  /dashboard
- Stores:
  /stores, /stores/[storeId]
- Requests:
  /requests
- Support:
  /support
- Services request submission:
  Ads Launch, Account Management
Mobile voice:
- Option A (simplest): text-only in mobile.
- Option B: record audio -> POST /api/ai/stt -> text -> continue chat.

Set:
- EXPO_PUBLIC_API_BASE = Replit web URL

========================================================
AI ENDPOINTS (MUST IMPLEMENT)
========================================================
- POST /api/ai/store-chat
  Input: chat history + partial answers + language
  Output: either next question(s) OR final StoreConfig JSON
  Constraint: Max 6–8 questions total

- POST /api/ai/campaign-brief
  Input: goal, budget, platforms, country, industry, timeframe, language
  Output: structured JSON brief + estimated KPI ranges (clearly marked estimates)

- POST /api/ai/account-management-brief
  Input: brand, goals, platforms, posting frequency, market, language
  Output: structured JSON brief (deliverables + timeline + KPIs)

- (Optional) POST /api/ai/stt
  audio -> text

IMPORTANT:
AI must output JSON only (no HTML). UI renders from templates.

========================================================
INTEGRATIONS LAYER (MODULAR) — REQUIRED
========================================================
Create adapter interfaces (even if stubbed in MVP):
- PaymentAdapter
- ShippingAdapter
- WarehouseAdapter

Integration Center (/admin/integrations):
- Provider Catalog (placeholders until real providers selected)
- Country mapping defaults (KW/SA/Other)
- Credentials vault (env-based) + “Test Connection” (stub ok)
- Activation templates/checklists per provider
- Event logs + webhook logs (MVP stub ok)

Store integration statuses:
- paymentsStatus / shippingStatus / warehousingStatus:
  not_requested | pending | approved | live

========================================================
ADMIN DASHBOARD (MUST HAVE) — REQUESTS + CONTENT + BLOG + SUPPORT
========================================================
/admin sections (in order):
1) Overview
2) New Requests (Leads & Tickets)
3) Stores
4) Customers
5) Integrations
6) Commission & Settlements
7) Content Studio (Launch New Pages)
8) Blog / Articles
9) Support Center
10) Settings + Audit Logs

Content Studio:
- Admin can create/edit/publish pages without code:
  pages: slug, titleAr/En, metaTitleAr/En, metaDescAr/En, contentJson, status(draft/published)
  Public route: /p/[slug]
- Must support bilingual + RTL
- Must appear in sitemap

Blog:
- CRUD posts AR/EN, /insights + /insights/[slug]
- Article schema for posts

Support Center:
- Customer creates support tickets in /portal/support
- Admin can assign, update status, add internal notes, reply templates

========================================================
DATA MODELS (PRISMA) — MINIMUM REQUIRED
========================================================
User:
- id, email/phone, role(USER|ADMIN), createdAt

Store:
- id, ownerId?, sessionId?, slug, storeName, country,
  storeConfigJson, status(DEMO|PENDING_ACTIVATION|LIVE|SUSPENDED),
  commissionRatePercent, paymentsStatus, shippingStatus, warehousingStatus, createdAt

Product:
- id, storeId, nameAr, nameEn, descAr, descEn, price, imagesJson, createdAt
(UNLIMITED products, no limits)

Lead:
- id, type(store_activation|ads_launch|account_mgmt),
  userId?, name, whatsapp, email?, country, storeId?, payloadJson,
  status(New|Contacted|Qualified|Closed), createdAt

Ticket:
- id, storeId?, userId?, type, status(Pending|Approved|InSetup|Live|Rejected),
  notesJson, createdAt, updatedAt

SupportTicket:
- id, userId, storeId?, subject, message, priority, status(Open|InProgress|Resolved),
  assignedTo?, createdAt, updatedAt

Content:
- Partner, TeamMember, CaseStudy, BlogPost, Page

Audit + Integration:
- AuditLog(actorId, action, entityType, entityId, beforeJson, afterJson, createdAt)
- IntegrationEvent, WebhookEvent (MVP acceptable stubs)

========================================================
SEO (WEB) — REQUIRED
========================================================
- Full i18n AR/EN + RTL
- hreflang tags
- sitemap.xml + robots.txt
- schema.org: Organization + LocalBusiness
- Accessible mic controls (aria labels)
- Fast performance, clean loading states

========================================================
WHATSAPP PREFILLED MESSAGE TEMPLATES (MUST USE MEDIA TREND NUMBER)
========================================================
After user submits request, show WhatsApp button linking to:
https://wa.me/9656566179840?text={ENCODED_TEXT}

Arabic template:
"مرحباً فريق Media Trend، قدمت طلب تفعيل للمتجر.
اسم المتجر: {storeName}
الدولة: {country}
المطلوب: {activations}
رابط المتجر التجريبي: {demoLink}
رقم التواصل: {whatsapp}"

English template:
"Hi Media Trend team, I submitted a store activation request.
Store: {storeName}
Country: {country}
Requested: {activations}
Demo link: {demoLink}
Contact: {whatsapp}"

========================================================
DELIVERABLES
========================================================
- Working Replit monorepo (web + mobile)
- Prisma migrations + seed data (partners/team/work/blog)
- All flows working end-to-end:
  demo store -> login -> submit request -> shows in admin -> admin updates statuses -> user sees updates
  ads launch request -> admin follow-up
  account management request -> admin follow-up
  support tickets -> user + admin
- README with run steps:
  - web dev
  - mobile dev (Expo QR)
  - env vars list
